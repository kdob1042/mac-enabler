# Merge these user-level defaults into ~/.codex/config.toml.
# This is a snippet, not a project-local .codex/config.toml.
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[agents]
max_concurrent_threads_per_session = 2
